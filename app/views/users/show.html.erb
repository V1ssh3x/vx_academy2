<h1>Профіль користувача</h1>

<p><strong>Ім'я:</strong> <%= @user.name %></p>
<p><strong>Email:</strong> <%= @user.email %></p>

<% if @user.avatar.attached? %>
  <div>
    <h3>Аватар:</h3>
    <div class="avatar">
      <%= image_tag @user.avatar %>
    </div>
  </div>
<% end %>

<%= link_to 'Редагувати профіль', edit_user_path(@user) %>
